const i18n = {
    de: {
        subtitle: "Echtzeit-Stromverbrauchs- & Kostenanalyse",
        donate: "☕ Spenden",
        verified_supporter: "⭐ Verified Supporter",
        logging_active: "Protokollierung aktiv",
        logging_paused: "Protokollierung pausiert",
        settings: "⚙️ Einstellungen",
        export_csv: "📥 CSV Export",
        hero_title: "Aktueller Gesamtverbrauch",
        hero_footer: "Inkl. ~12% Netzteil-Verlust & Monitor",
        today_title: "Verbrauch Heute",
        today_energy: "Energie",
        today_cost: "Geschätzte Kosten",
        total_title: "Gesamtverbrauch (Protokoll)",
        total_energy: "Gesamt Energie",
        total_cost: "Gesamtkosten",
        forecast_title: "Prognose (Aktuelle Last)",
        cost_per_hour: "Kosten pro Stunde",
        cost_per_day: "Prognose 24h",
        comp_title: "Komponenten-Leistungsaufnahme",
        comp_cpu: "Prozessor (CPU)",
        comp_gpu: "Grafikkarte",
        comp_monitor: "Bildschirm",
        chart_title: "Echtzeit-Leistungsverlauf",
        chart_subtitle: "Leistungsaufnahme der letzten Minuten (Watt)",
        btn_live: "Echtzeit (60s)",
        btn_hourly: "Heute (Stündlich)",
        btn_daily: "Tagesübersicht",
        label_language: "Sprache / Language",
        label_price: "Strompreis (€ / kWh)"
    },
    en: {
        subtitle: "Real-time Power Usage & Cost Analytics",
        donate: "☕ Donate",
        verified_supporter: "⭐ Verified Supporter",
        logging_active: "Logging active",
        logging_paused: "Logging paused",
        settings: "⚙️ Settings",
        export_csv: "📥 Export CSV",
        hero_title: "Current Total Consumption",
        hero_footer: "Incl. ~12% PSU efficiency loss & Monitor",
        today_title: "Today's Consumption",
        today_energy: "Energy",
        today_cost: "Estimated Cost",
        total_title: "Total Consumption (Log)",
        total_energy: "Total Energy",
        total_cost: "Total Cost",
        forecast_title: "Forecast (Current Load)",
        cost_per_hour: "Cost per Hour",
        cost_per_day: "Forecast 24h",
        comp_title: "Component Power Draw",
        comp_cpu: "Processor (CPU)",
        comp_gpu: "Graphics Card",
        comp_monitor: "Display / Monitor",
        chart_title: "Real-Time Power History",
        chart_subtitle: "Power consumption in recent minutes (Watts)",
        btn_live: "Real-time (60s)",
        btn_hourly: "Today (Hourly)",
        btn_daily: "Daily Overview",
        label_language: "Language / Sprache",
        label_price: "Electricity Price (€ / kWh)"
    },
    es: {
        subtitle: "Análisis de consumo eléctrico y costes en tiempo real",
        donate: "☕ Donar",
        verified_supporter: "⭐ Verified Supporter",
        logging_active: "Registro activo",
        logging_paused: "Registro pausado",
        settings: "⚙️ Ajustes",
        export_csv: "📥 Exportar CSV",
        hero_title: "Consumo Total Actual",
        hero_footer: "Incl. ~12% pérdida de fuente y monitor",
        today_title: "Consumo de Hoy",
        today_energy: "Energía",
        today_cost: "Coste Estimado",
        total_title: "Consumo Total (Historial)",
        total_energy: "Energía Total",
        total_cost: "Coste Total",
        forecast_title: "Pronóstico (Carga Actual)",
        cost_per_hour: "Coste por hora",
        cost_per_day: "Pronóstico 24h",
        comp_title: "Consumo por Componentes",
        comp_cpu: "Procesador (CPU)",
        comp_gpu: "Tarjeta Gráfica",
        comp_monitor: "Pantalla / Monitor",
        chart_title: "Histórico de Potencia en Tiempo Real",
        chart_subtitle: "Consumo de energía en los últimos minutos (Vatios)",
        btn_live: "Tiempo real (60s)",
        btn_hourly: "Hoy (Por hora)",
        btn_daily: "Resumen diario",
        label_language: "Idioma / Language",
        label_price: "Precio de luz (€ / kWh)"
    },
    fr: {
        subtitle: "Analyse en temps réel de la consommation et des coûts électriques",
        donate: "☕ Faire un don",
        verified_supporter: "⭐ Verified Supporter",
        logging_active: "Enregistrement actif",
        logging_paused: "Enregistrement en pause",
        settings: "⚙️ Paramètres",
        export_csv: "📥 Exporter CSV",
        hero_title: "Consommation Totale Actuelle",
        hero_footer: "Incl. ~12% perte d'alimentation & écran",
        today_title: "Consommation d'Aujourd'hui",
        today_energy: "Énergie",
        today_cost: "Coût Estimé",
        total_title: "Consommation Totale (Historique)",
        total_energy: "Énergie Totale",
        total_cost: "Coût Total",
        forecast_title: "Prévision (Charge Actuelle)",
        cost_per_hour: "Coût par heure",
        cost_per_day: "Prévision 24h",
        comp_title: "Consommation des Composants",
        comp_cpu: "Processeur (CPU)",
        comp_gpu: "Carte Graphique",
        comp_monitor: "Écran / Moniteur",
        chart_title: "Historique de Puissance en Temps Réel",
        chart_subtitle: "Consommation électrique des dernières minutes (Watts)",
        btn_live: "Temps réel (60s)",
        btn_hourly: "Aujourd'hui (Par heure)",
        btn_daily: "Aperçu quotidien",
        label_language: "Langue / Language",
        label_price: "Prix de l'électricité (€ / kWh)"
    },
    it: {
        subtitle: "Analisi in tempo reale dei consumi elettrici e dei costi",
        donate: "☕ Donazione",
        verified_supporter: "⭐ Verified Supporter",
        logging_active: "Registrazione attiva",
        logging_paused: "Registrazione in pausa",
        settings: "⚙️ Impostazioni",
        export_csv: "📥 Esporta CSV",
        hero_title: "Consumo Totale Attuale",
        hero_footer: "Incl. ~12% perdita alimentatore e monitor",
        today_title: "Consumo di Oggi",
        today_energy: "Energia",
        today_cost: "Costo Stimato",
        total_title: "Consumo Totale (Cronologia)",
        total_energy: "Energia Totale",
        total_cost: "Costo Totale",
        forecast_title: "Previsione (Carico Attuale)",
        cost_per_hour: "Costo all'ora",
        cost_per_day: "Previsione 24h",
        comp_title: "Consumo dei Componenti",
        comp_cpu: "Processore (CPU)",
        comp_gpu: "Scheda Grafica",
        comp_monitor: "Schermo / Monitor",
        chart_title: "Cronologia Potenza in Tempo Reale",
        chart_subtitle: "Consumo energetico negli ultimi minuti (Watt)",
        btn_live: "Tempo reale (60s)",
        btn_hourly: "Oggi (Orario)",
        btn_daily: "Panoramica giornaliera",
        label_language: "Lingua / Language",
        label_price: "Prezzo energia (€ / kWh)"
    }
};

let currentLang = 'de';

function applyLanguage(lang) {
    if (!i18n[lang]) lang = 'de';
    currentLang = lang;
    const dict = i18n[lang];
    document.querySelectorAll('[data-i18n]').forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (dict[key]) {
            if (el.tagName === 'INPUT' && el.type === 'placeholder') {
                el.placeholder = dict[key];
            } else {
                el.textContent = dict[key];
            }
        }
    });
}

document.addEventListener('DOMContentLoaded', () => {
    initChart();
    fetchTelemetry();
    fetchHistoryData(currentPeriod);
    checkForUpdates();

    // Poll telemetry every 1500 ms
    pollTimer = setInterval(fetchTelemetry, 1500);

    setupEventListeners();
});

async function checkForUpdates() {
    try {
        const res = await fetch('/api/update_check');
        if (!res.ok) return;
        const data = await res.json();
        if (data.update_available) {
            const banner = document.getElementById('update-banner');
            const versionTag = document.getElementById('update-version-tag');
            const link = document.getElementById('update-download-link');
            
            if (banner && versionTag && link) {
                versionTag.textContent = data.latest_version;
                link.href = data.download_url;
                banner.style.display = 'flex';
            }
        }
    } catch (e) {
        console.error("Update check error", e);
    }
}

function initChart() {
    const ctx = document.getElementById('liveChart').getContext('2d');
    
    Chart.defaults.color = '#94a3b8';
    Chart.defaults.font.family = 'Outfit, sans-serif';

    liveChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: [],
            datasets: [
                {
                    label: 'Gesamt (W)',
                    data: [],
                    borderColor: '#00f2fe',
                    backgroundColor: 'rgba(0, 242, 254, 0.08)',
                    fill: true,
                    tension: 0.35,
                    borderWidth: 2,
                    pointRadius: 2
                },
                {
                    label: 'GPU RTX 3060 (W)',
                    data: [],
                    borderColor: '#8b5cf6',
                    backgroundColor: 'transparent',
                    borderWidth: 1.5,
                    pointRadius: 0
                },
                {
                    label: 'CPU (W)',
                    data: [],
                    borderColor: '#4facfe',
                    backgroundColor: 'transparent',
                    borderWidth: 1.5,
                    pointRadius: 0
                },
                {
                    label: 'Monitor (W)',
                    data: [],
                    borderColor: '#f59e0b',
                    backgroundColor: 'transparent',
                    borderWidth: 1.5,
                    pointRadius: 0
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            animation: { duration: 300 },
            scales: {
                x: {
                    grid: { color: 'rgba(255, 255, 255, 0.04)' }
                },
                y: {
                    grid: { color: 'rgba(255, 255, 255, 0.06)' },
                    suggestedMin: 0,
                    suggestedMax: 200,
                    title: { display: true, text: 'Leistungsaufnahme (Watt)' }
                }
            },
            plugins: {
                legend: {
                    position: 'top',
                    labels: { boxWidth: 12, usePointStyle: true }
                },
                tooltip: {
                    mode: 'index',
                    intersect: false,
                    backgroundColor: 'rgba(10, 14, 26, 0.9)',
                    borderColor: 'rgba(255, 255, 255, 0.1)',
                    borderWidth: 1
                }
            }
        }
    });
}

async function fetchTelemetry() {
    try {
        const response = await fetch('/api/telemetry');
        if (!response.ok) return;
        const data = await response.json();

        // Update UI tiles
        document.getElementById('val-total-w').textContent = data.total_w.toFixed(1);
        document.getElementById('val-cpu-w').textContent = data.cpu_w.toFixed(1);
        document.getElementById('val-gpu-w').textContent = data.gpu_w.toFixed(1);
        document.getElementById('val-mon-w').textContent = data.monitor_w.toFixed(1);

        if (data.connected_monitors && data.connected_monitors.length > 0) {
            document.getElementById('mon-detected-names').textContent = data.connected_monitors.join(', ');
        }

        document.getElementById('val-today-kwh').textContent = data.today_kwh.toFixed(4);
        document.getElementById('val-today-cost').textContent = data.today_cost.toFixed(2);
        document.getElementById('val-all-kwh').textContent = data.all_kwh.toFixed(4);
        document.getElementById('val-all-cost').textContent = data.all_cost.toFixed(2);

        // Calculate current cost forecast per hour & per day
        currentPrice = parseFloat(data.settings?.electricity_price || 0.35);
        const kw = data.total_w / 1000.0;
        const costPerHour = kw * currentPrice;
        const costPerDay = costPerHour * 24.0;

        document.getElementById('val-cost-hour').textContent = costPerHour.toFixed(3);
        document.getElementById('val-cost-day').textContent = costPerDay.toFixed(2);

        // Update meter bars
        const maxWattEstimate = 350.0;
        const totalPct = Math.min(100, (data.total_w / maxWattEstimate) * 100);
        document.getElementById('total-meter-fill').style.width = `${totalPct}%`;

        document.getElementById('cpu-bar-fill').style.width = `${Math.min(100, (data.cpu_w / 120) * 100)}%`;
        document.getElementById('gpu-bar-fill').style.width = `${Math.min(100, (data.gpu_w / 170) * 100)}%`;
        document.getElementById('mon-bar-fill').style.width = `${Math.min(100, (data.monitor_w / 50) * 100)}%`;

        // Update logging badge & Donate button / Supporter badge
        const badge = document.getElementById('logging-badge');
        const badgeText = document.getElementById('logging-status-text');
        const donateBtn = document.getElementById('donate-btn');
        const supporterBadge = document.getElementById('supporter-badge');

        if (data.settings && data.settings.language && data.settings.language !== currentLang) {
            applyLanguage(data.settings.language);
        }

        if (data.is_supporter) {
            donateBtn.style.display = 'none';
            supporterBadge.style.display = 'inline-flex';
        } else if (data.settings && data.settings.donate_url && data.settings.donate_url.trim() !== '') {
            donateBtn.href = data.settings.donate_url;
            donateBtn.style.display = 'inline-flex';
            supporterBadge.style.display = 'none';
        } else {
            donateBtn.style.display = 'none';
            supporterBadge.style.display = 'none';
        }

        if (data.is_logging) {
            badge.style.opacity = '1';
            badgeText.textContent = 'Protokollierung aktiv';
        } else {
            badge.style.opacity = '0.5';
            badgeText.textContent = 'Protokollierung pausiert';
        }

        // If in "recent" chart mode, append live telemetry point
        if (currentPeriod === 'recent') {
            const timeStr = new Date().toLocaleTimeString('de-DE');
            appendLivePoint(timeStr, data.total_w, data.gpu_w, data.cpu_w, data.monitor_w);
        }
    } catch (e) {
        console.error("Telemetry fetch error", e);
    }
}

function appendLivePoint(label, total, gpu, cpu, mon) {
    if (!liveChart) return;
    const labels = liveChart.data.labels;
    const ds = liveChart.data.datasets;

    labels.push(label);
    ds[0].data.push(total);
    ds[1].data.push(gpu);
    ds[2].data.push(cpu);
    ds[3].data.push(mon);

    if (labels.length > 40) {
        labels.shift();
        ds[0].data.shift();
        ds[1].data.shift();
        ds[2].data.shift();
        ds[3].data.shift();
    }
    liveChart.update('none');
}

async function fetchHistoryData(period) {
    try {
        const res = await fetch(`/api/history?period=${period}`);
        if (!res.ok) return;
        const rows = await res.json();

        if (!liveChart) return;

        if (period === 'recent') {
            liveChart.config.type = 'line';
            liveChart.data.labels = rows.map(r => r.time_label);
            liveChart.data.datasets[0].data = rows.map(r => r.total_w);
            liveChart.data.datasets[1].data = rows.map(r => r.gpu_w);
            liveChart.data.datasets[2].data = rows.map(r => r.cpu_w);
            liveChart.data.datasets[3].data = rows.map(r => r.monitor_w);
        } else if (period === 'today') {
            liveChart.config.type = 'bar';
            liveChart.data.labels = rows.map(r => r.time_label);
            liveChart.data.datasets[0].data = rows.map(r => r.avg_total_w);
            liveChart.data.datasets[1].data = rows.map(r => r.avg_gpu_w);
            liveChart.data.datasets[2].data = rows.map(r => r.avg_cpu_w);
            liveChart.data.datasets[3].data = [];
        } else if (period === 'daily') {
            liveChart.config.type = 'bar';
            liveChart.data.labels = rows.map(r => r.time_label);
            liveChart.data.datasets[0].data = rows.map(r => r.avg_total_w);
            liveChart.data.datasets[1].data = [];
            liveChart.data.datasets[2].data = [];
            liveChart.data.datasets[3].data = [];
        }
        liveChart.update();
    } catch (e) {
        console.error("History fetch error", e);
    }
}

function setupEventListeners() {
    // Chart Tab Buttons
    document.querySelectorAll('.tab-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            e.target.classList.add('active');
            currentPeriod = e.target.getAttribute('data-period');
            fetchHistoryData(currentPeriod);
        });
    });

    // CSV Export
    document.getElementById('export-csv-btn').addEventListener('click', () => {
        window.location.href = '/api/export';
    });

    // Donate Button Click
    const donateBtn = document.getElementById('donate-btn');
    if (donateBtn) {
        donateBtn.addEventListener('click', () => {
            donateBtn.textContent = '❤️ Vielen Dank!';
            setTimeout(() => {
                donateBtn.style.opacity = '0.5';
            }, 3000);
        });
    }

    // Modal Settings
    const modal = document.getElementById('settings-modal');
    document.getElementById('open-settings-btn').addEventListener('click', async () => {
        const res = await fetch('/api/telemetry');
        const data = await res.json();
        document.getElementById('setting-language').value = s.language || 'de';
        document.getElementById('input-price').value = s.electricity_price || 0.35;
        document.getElementById('input-mon-w').value = s.monitor_wattage || 'auto';
        document.getElementById('input-cpu-tdp').value = s.cpu_tdp || 95;
        document.getElementById('input-psu-eff').value = s.psu_efficiency || 88;
        document.getElementById('input-interval').value = s.logging_interval || 2.0;
        document.getElementById('input-donate-url').value = s.donate_url || 'https://ko-fi.com/smallstep';
        document.getElementById('input-supporter-code').value = s.supporter_code || '';
        document.getElementById('input-is-logging').checked = s.is_logging === 'true';

        modal.classList.add('active');
    });

    document.getElementById('setting-language').addEventListener('change', (e) => {
        applyLanguage(e.target.value);
    });

    document.getElementById('close-settings-btn').addEventListener('click', () => modal.classList.remove('active'));
    document.getElementById('cancel-settings-btn').addEventListener('click', () => modal.classList.remove('active'));

    document.getElementById('save-settings-btn').addEventListener('click', async () => {
        const langVal = document.getElementById('setting-language').value;
        const payload = {
            language: langVal,
            electricity_price: document.getElementById('input-price').value,
            monitor_wattage: document.getElementById('input-mon-w').value,
            cpu_tdp: document.getElementById('input-cpu-tdp').value,
            psu_efficiency: document.getElementById('input-psu-eff').value,
            logging_interval: document.getElementById('input-interval').value,
            donate_url: document.getElementById('input-donate-url').value,
            supporter_code: document.getElementById('input-supporter-code').value,
            is_logging: document.getElementById('input-is-logging').checked ? 'true' : 'false'
        };

        applyLanguage(langVal);

        await fetch('/api/settings', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(payload)
        });

        modal.classList.remove('active');
        fetchTelemetry();
    });

    document.getElementById('reset-logs-btn').addEventListener('click', async () => {
        const confirmMsg = currentLang === 'en' ? "Do you really want to delete all logged power usage history?" : "Möchtest du wirklich alle aufgezeichneten Verbrauchs-Protokolle löschen?";
        if (confirm(confirmMsg)) {
            await fetch('/api/reset', { method: 'POST' });
            modal.classList.remove('active');
            fetchTelemetry();
            fetchHistoryData(currentPeriod);
        }
    });
}
